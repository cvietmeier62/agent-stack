# Conventions

- Dark mode only (no light toggle)
- JetBrains Mono for code/timestamps. Geist Sans for body.
- AI Elements for ALL AI-generated text — never raw markdown
- shadcn/ui components — don't build custom when primitive exists
- Skeleton loading state on every async component
- This is a DEMO — prioritize speed and clarity over features
