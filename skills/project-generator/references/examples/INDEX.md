# Example Outputs

Three complete project specs generated by this skill. Each is the FULL output folder — every file the project-generator produces. Use these as calibration for quality, structure, and level of detail.

## 1. chatbot-demo/ (simple — 10 tasks)

Minimal Strands + Next.js chatbot for demo. The simplest possible output.

- **Stack:** Strands Agents SDK + Next.js 16 + AI Elements
- **Deploy:** AWS (local + Docker)
- **Complexity:** 10 tasks, 5 phases
- **Use as reference when:** generating specs for demos, prototypes, simple apps

## 2. kiro-sales-aws/ (complex — 25 tasks)

Full product: AI sales advisor with IDE-style layout, S3 Files, AgentCore Runtime, CDK.

- **Stack:** Strands + AgentCore + S3 Files + Next.js 16 + AI Elements
- **Deploy:** All AWS (AgentCore + Amplify/ECS + CDK)
- **Complexity:** 25 tasks, 7 phases
- **Use as reference when:** generating specs for full products deploying to AWS

## 3. kiro-sales-vercel/ (complex — 20 tasks)

Same product concept as kiro-sales-aws but deployed on Vercel + Supabase. Shows how the same idea gets different specs for different infrastructure.

- **Stack:** Claude Agent SDK + Vercel + Supabase + Clerk + AI Elements
- **Deploy:** Vercel (frontend) + Docker/local (harness)
- **Complexity:** 20 tasks, 7 phases
- **Use as reference when:** generating specs for Vercel-deployed products

## How to Use

1. Determine which example is closest to the new project
2. Read that example's full folder — CLAUDE.md, rules/, DESIGN.md, TASKS.md
3. Generate output that matches the same quality: specificity, conciseness, structure
4. Check your output's line counts against the example's line counts
